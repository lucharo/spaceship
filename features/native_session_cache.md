# Native session cache

Show the last known provider-native session metadata immediately, then refresh it from the provider in place. The cache is disposable presentation state: it never contains transcript bodies, never becomes authoritative, and is partitioned by provider, host, and active or archived catalogue.
