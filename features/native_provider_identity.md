# Native provider identity

Native thread rows should use the owning provider's visual identity through the provider plugin rather than a generic chat icon. Core keeps a neutral fallback and the compact row contract; provider-specific marks and accessible labels are tracked in [#15](https://github.com/lucharo/spaceship/issues/15).
