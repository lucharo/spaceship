# Minimal by default

The default interface shows the smallest useful set: new thread, skills, provider-native threads, and settings. Plugins, automations, mobile, remote-host controls, experimental tools, duplicate thread catalogues, and providers without an equivalent native adapter stay out of the primary path until the user explicitly enables or opens them; every new default-surface item must justify the attention it consumes.
