# Short name

Spaceship's short name is `sp`. Do not use `ss`: it is less distinctive and is already associated with unrelated system tooling. This decision names the project; it does not promise that an `sp` executable alias is installed.
