# Remote native hosts

Remote machines should expose their native session catalogues through the same host-scoped provider adapter, with clear offline states and no automatic transcript copying to the local machine. The host picker and remote continuation flow are tracked in [#3](https://github.com/lucharo/spaceship/issues/3).
