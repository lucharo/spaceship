# Native skill invocation

Installed skills are available through both `$name` and `/name` composer syntax. Dollar completion is skill-only, while slash completion keeps BB commands and skills together; either form preserves the user's chosen prefix when inserted.
