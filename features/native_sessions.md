# Native sessions

Spaceship's main Threads list is the provider-native catalogue, not a second Spaceship history beside it. The Codex provider plugin lists active app-server threads, opens and continues the selected native ID, and offers pins, grouping, search, archive, and right-click actions; transcript bodies are read only after the user opens one session, while import or migration remains explicit.
