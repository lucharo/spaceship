# Native sessions

Spaceship lists provider-native session metadata, opens a selected native session, and continues it through the provider's own interface using the same native ID. Catalogue browsing stays metadata-only, transcript bodies are read only after the user opens a session, and import or migration is always a separate explicit action.
