# Minimal by default

The default interface shows the smallest useful set: new thread, skills, native sessions, current threads, and settings. Plugins, automations, mobile, remote-host controls, experimental tools, and unavailable providers stay out of primary navigation until the user explicitly enables or opens them; every new default-surface item must justify the attention it consumes.
