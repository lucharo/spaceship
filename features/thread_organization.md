# Thread organisation

The native Threads list can be organised chronologically or by working project, with visible recency, persistent pins, and independently collapsible sections. These choices are local presentation state only and never alter the provider's native thread.
